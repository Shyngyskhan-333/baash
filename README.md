```bash
git clone https://github.com/YOUR_ORG/legal-entropy
cd legal-entropy
python -m venv venv && source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env  # заполните ключи Azure
```
